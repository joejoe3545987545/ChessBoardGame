# 开发日志 — 2026-06-16

## 已完成

### 卡牌系统 — 核心闭环
- [x] **出牌前 AP 检查**：拖牌到 CardReader 时验证 `hasValidActionPoint(false)`，无权限则弹回卡槽
- [x] **湮灭完成触发效果**：展示结束后 → `consumeActionPoint(false)` → `applyCardEffect()` → `settleActionPoints()`
- [x] **"连击"卡牌效果**：`FORCE_DROP` 完整实现，赠送 2 个仅下棋行动点
- [x] **"隐忍"卡牌效果**：`CHANGE_WIN_RULE` 改为修改**敌方**胜利条件为 6 子连星
- [x] **每方独立胜利条件**：`Chessboard::winCondition[player]`，checkWin 改为读取此值
- [x] **卡牌库更新**：删"规则加固"，保留"连击"和"隐忍"
- [x] **重新开局 AP 初始化修复**

### 卡牌特效
- [x] **湮灭闪烁修复**：离散 50 段量化 → 连续插值，消除帧率拍频；尾部 0.4s 软淡出 + 最后 20ms 强制归零
- [x] **出生动画闪烁修复**：同样改为连续插值
- [x] **卡牌展示动画**：4 阶段（PAUSE 0.3s → APPEAR 1.2s → DISPLAY 1.2s → FADE_OUT 0.4s），缓出缩放 + 白光 + 文字等比缩放
- [x] **展示文字对齐**：统一缩放因子 `SCALE_RATIO = 0.38/0.18`，字号/偏移全部按此比例
- [x] **展示缩放缓出**：`exp(-t*8)` 指数曲线

### 双卡槽系统
- [x] **第二卡槽**：(1080, 486)，下方空置备用
- [x] **多卡堆叠渲染**：每张卡 +5px 偏移，底卡在下、新卡在上
- [x] **双槽存放**：吸附态点击槽 1/槽 2 区域 → 卡牌移入对应槽位
- [x] **存入即栈顶**：存放时自动把卡牌移到 hand 末尾，确保在上层
- [x] **双槽独立拾取**：每槽顶卡独立可被吸附，显式计算包围盒，无 lambda
- [x] **每张卡都渲染文字**：不再仅栈顶，渲染顺序天然遮挡
- [x] **回弹动画修复**：`isReturningToSlot` 时 activeIdx 保持 attachedCardIndex

### 渲染重构
- [x] **统一 activeIdx**：吸附/湮灭/回弹/默认四种状态统一用 activeIdx 标识活跃卡
- [x] **精灵位置保存恢复**：静态循环前后保存/恢复 `newCardSprite` 位置，防残留坐标污染状态机
- [x] **文字指向活跃卡**：状态机文字读 `hand[activeIdx]` 而非 `hand.back()`
- [x] **出生动画触发**：改用显式 `newCardJustDrawn` 标记代替 `handSize` 对比

### 音乐系统
- [x] **动态战斗音乐**：4 状态状态机（NORMAL ↔ SWITCHING_TO_02 ↔ BATTLE_02 ↔ SWITCHING_TO_01）
- [x] **1s crossfade**：battle_01 ↔ battle_02 音量渐变
- [x] **CardReader 触发切歌** + **四子连星触发危机延长 30s**
- [x] **battle_02 无时长上限**，循环播放，计时归零切回 battle_01

### 回合计时暂停
- [x] **卡牌动画期间不计时**：CardReader 触发时暂停，FADE_OUT 结束恢复
- [x] **全局忙碌锁 `isBusyAnimating`**：动画期间禁止玩家操作和 AI 行动

### 项目文档体系
- [x] `devlog/` 开发日志、`docs/` 需求/技术/设计/步骤文档、`CLAUDE.md` 指引

## 待办

- [ ] 实现 REMOVE_OPPONENT 卡牌效果
- [ ] 实现 EXTRA_TURN 卡牌效果
- [ ] 扩展牌库内容（JSON 加载）
- [ ] PVE 模式下 AI 出牌逻辑
- [ ] AI 移动也检测 pattern 触发抽卡

## 备注

- 行动点框架（三模式判定、智能消耗、总体结算）由前一轮完成
- CardReader 湮灭 = 出牌，展示结束 = 效果触发
- `activeIdx` 是渲染系统的核心抽象，四种互动状态统一管理
- SFML 3.0 坐标体系：`FloatRect({pos}, {size})`，`contains(Vector2f)`
