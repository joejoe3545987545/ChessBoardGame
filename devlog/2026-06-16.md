# 开发日志 — 2026-06-16

## 已完成

### 卡牌出牌流程打通
- [x] **出牌前 AP 检查**：拖牌到 CardReader 时验证 `hasValidActionPoint(false)`，无权限则弹回卡槽
- [x] **湮灭完成触发效果**：动画结束后捕获卡牌 → `consumeActionPoint(false)` → `applyCardEffect()` → `settleActionPoints()`
- [x] **"连击"卡牌效果实现**：`FORCE_DROP` 效果完整实现，赠送 2 个仅下棋行动点（`addActionPoint(true, false)` × 2），其余 3 种效果预留框架
- [x] **重新开局 AP 初始化修复**：`handleHUDClick` 中"重新开局"按钮补充 `initActionPointsForTurn()` 调用

### 项目文档体系搭建
- [x] 创建 `devlog/` 开发日志文件夹
- [x] 创建 `docs/` 项目文档文件夹
- [x] 创建 `CLAUDE.md` AI 助手指引文件

### 湮灭特效修复
- [x] **根因定位**：PAUSE_FINAL 渲染通道中基底 alpha 衰减 + 白光双因子 (`whiteProgress * fadeProgress`) 相乘导致帧间亮度剧烈跳动
- [x] **修复方案**：镜像出生动画的双通道渲染模式
  - 基底：改为始终 `(255,255,255,255)` 不透明（与出生动画一致）
  - 白光：改为 `whiteProgress = t / 1.0s` 线性 0→1（出生动画为 1→0 的反向）
  - 移除 `fadeProgress` 因子，卡牌消失仅由 50 段阶梯裁剪驱动
  - 文字独立 0.8s 淡出

### 修改文件
| 文件 | 改动 |
|------|------|
| `include/GameEngine.h` | +11 行：`applyCardEffect()` 声明 + `CardShowcaseState` 枚举 + 3 个展示成员变量 |
| `src/GameEngine.cpp` | +140 行：AP 检查、效果触发、重启修复、效果分发、湮灭特效重写、展示动画 |

### 卡牌效果展示动画（Showcase）
- [x] **4 阶段展示状态机**：PAUSE(0.5s) → APPEAR(1.2s, 缓出缩放+阶梯+白光) → DISPLAY(2s) → FADE_OUT(1s, 亮白淡出)
- [x] 效果触发从湮灭完成即时触发 → FADE_OUT 结束后延迟触发
- [x] 展示卡牌位于画面正中央 (640,360)，缩放 0.38（约 304×422px）
- [x] 复用出生动画的双通道渲染模式（BlendAlpha + BlendAdd）
- [x] 重新开局时重置展示状态

## 待办

- [ ] 实现 CHANGE_WIN_RULE 卡牌效果（修改胜利条件为 6 子）
- [ ] 实现 REMOVE_OPPONENT 卡牌效果（移除对方棋子）
- [ ] 实现 EXTRA_TURN 卡牌效果（额外完整回合）
- [ ] 扩展牌库内容（目前仅 2 张测试卡牌）
- [ ] PVE 模式下 AI 出牌逻辑
- [ ] 手牌数量 UI 显示（多张手牌的视觉呈现）
- [ ] 卡牌选择系统（多张手牌时选择出哪张）

## 备注

- 行动点框架（三模式判定、智能消耗、总体结算）已于之前完成，本次改动是在此基础上打通卡牌出牌的完整链路
- CardReader 湮灭 = 出牌的设计已确定，下次实现新卡牌效果时只需在 `applyCardEffect()` 中添加 case 分支
