# 开发日志 — 2026-06-19

## 已完成

### AI 卡牌系统
- [x] **共享牌库**：`DeckManager` 新增 `aiHand`，AI 与玩家从同一套牌库抽牌
- [x] **AI 抽牌**：AI 下棋后 pattern 检测 → 三连/四连自动 `drawCardForAI()`
- [x] **AI 出牌动画**：牌从 CardPortal(1230) 升起→停留→滑入 CardReader→湮灭→展示→效果
- [x] **AI 回合重构**：支持多 AP 连续行动（FORCE_DROP 后连下多步）
- [x] **AI 卡牌决策**：`AIPlayer` 新增 `shouldPlayCard`/`chooseCardToPlay`/`evaluateCard`，难度影响出牌阈值
- [x] **AI CONVERT_PIECE 自动选子**：复用动画流程，自动选己方最弱+敌方最强
- [x] **AI 四子连星不影响战斗音乐**

### 暂停菜单系统
- [x] **ESC 暂停**：移除 Escape 退出和底部 HUD 按钮，对局中 ESC 切换暂停
- [x] **暂停覆盖层**：半透明黑色遮罩 0.5s 淡入/淡出
- [x] **暂停按钮**：UI_Frame 四按钮（认输/重开/返回主页/退出），悬停抖动，独立偏移布局
- [x] **下落动画**：SettingsMenu + 按钮从上方 1200px 落下，0.5s cubic ease-out + 透明度淡入
- [x] **SettingsMenu.png** 居中偏上显示

### 界面统一
- [x] **所有界面 Universe 背景**：主菜单/PVE配置/游戏设置/规则说明/历史记录/对局/GAME_OVER
- [x] **Universe 帧缓存**：仅在帧号变化时加载 JPEG，避免每帧磁盘 I/O
- [x] **对战配置 UI 标准化**：UI_Frame + cardFont，主菜单同款检测区
- [x] **游戏设置 UI 标准化**：同上
- [x] **规则说明 UI 标准化**：Universe 背景 + cardFont 白色正文
- [x] **历史记录 UI 标准化**：Universe 背景 + cardFont 居中列表
- [x] **GAME_OVER UI 标准化**：Universe 背景 + cardFont + UI_Frame 按钮

### 落子动画
- [x] **抛物线落子**：棋子从偏右偏上处 1.1 倍大小沿抛物线坠落，0.35s cubic ease-out
- [x] 复盘时跳过落子动画

### 卡牌系统增强
- [x] **出牌动画闪烁修复**：PAUSE_AFTER→MOVE_TO_ZERO 过渡帧不再丢绘制
- [x] **乱码卡片方向平滑**：`lastMouseDir` 指数移动平均抑制单帧抖动
- [x] **乱码卡片渲染插值**：`glitchRenderPos` 平滑追赶物理位置，提高视觉帧率
- [x] **乱码文字池扩充**：加入疫病/隔离相关汉字
- [x] **牌库空时不再触发虚假出生动画**：检测手牌数变化后再设 `newCardJustDrawn`

### 疫病系统
- [x] **PLAGUE 卡牌**：指定敌方棋子感染，每回合 20% 销毁 + 四向传播（敌方60%/我方40%）
- [x] **QUARANTINE 卡牌**：4 回合后清除全部疫病标记
- [x] **潜伏期**：刚感染当回合不传播，下一回合进入活跃期
- [x] **感染标记**：患病棋子上方显示 `virus.png`
- [x] **`processInfection()`** 每回合开始时自动处理传播+销毁
- [x] **PIP 动画 3**：疫病传播演示

### 紫卡框架
- [x] **Card 结构体**：新增 `cardColor` 字段（0=橙卡，1=紫卡）
- [x] **紫卡纹理**：`Card_01.png`，自动选精灵渲染
- [x] **CardPortal 检测区**：`portalDetectionZone` 在 (300, 1060)，半径 240
- [x] **紫卡传送流程**：拖到 Portal 检测区→snap→0.2s 停顿→0.35s 下滑→进入敌方手牌
- [x] **AI 紫卡优先**：`evaluateCard` 紫卡返回 120 分，`shouldPlayBeforeDrop` 优先出

### 卡牌文字渲染优化
- [x] **文字跟随裁剪边缘出现**：动态阈值替代硬编码百分比
- [x] **逐行淡入**：每行出现时 0.1s 透明度淡入 + 亮→原色
- [x] **统一逐行渲染**：静态卡槽/退牌/出生/展示全部拆行独立 `sf::Text`，行距 26px 一致

### Bug 修复
- [x] GAME_OVER→MENU 按钮消失：`uiFrameSpr` alpha 残留修复
- [x] GAME_OVER→MENU 状态泄漏：全面重置游戏/动画/音乐状态
- [x] 对战配置检测区偏移：click handler 加入 `hyOff` 居中修正
- [x] AI 笼络后 `isSelectingPiece` 残留导致无法下棋
- [x] `settleActionPoints` 切回合时强制 `isAiThinking = false`

## 待办
- [ ] JSON 卡牌配置加载
- [ ] 具体紫卡效果实现
- [ ] AI Portal 紫卡发送动画

## 备注
- 紫卡测试卡"诅咒测试"已加入牌库（cardColor=1）
- 卡牌描述现全部用 `\n` 分隔，渲染时逐行拆分，行距 26px
- PIP 动画循环增至 3 个：读卡演示→棋盘演示→疫病传播
