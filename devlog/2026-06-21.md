# 开发日志 — 2026-06-21

## 已完成

### 新紫卡：以地事秦（YIDISHIQIN）
- [x] **卡牌效果**：持有者需在生效回合内传送一张橙卡回给出牌者，否则三回合不能出牌
- [x] **回应窗口**：收到后一回合内拖橙卡到 CardPortal 即为回应，走 Portal 传送动画
- [x] **惩罚机制**：未回应则三回合 `hasValidActionPoint(false)` 返回 false（禁出牌）
- [x] **紫卡保留**：惩罚期间紫卡保留在卡槽，倒计时归零后粉碎移除
- [x] **AI 逻辑**：70% 概率选橙卡送出回应，否则自动进入惩罚；回应仅回合首次判断
- [x] **Portal 纹理修复**：传送时根据 cardColor 选择橙/紫精灵，送橙卡不再变紫色
- [x] **文字池更新**：glitch 卡牌和 blindPool 加入以地事秦汉字

### 调试面板增强
- [x] **[只会下棋] 按钮**：AI 跳过所有出牌决策，只落子
- [x] **[只会出牌] 按钮**：AI 强制出牌（忽略策略评分），有手牌时跳过落子；兜底随机选卡
- [x] 两按钮互斥，F10 面板底部显示

### 卡牌动画修复
- [x] **卡牌出生精灵位置恢复**：`spB->setPosition(cardPos)` 防止碎片污染精灵位置
- [x] **旧出生辉光跳过**：`isAnimatingCard` 为 false 时跳过 BlendAdd，消除切回旧代码的闪变
- [x] **卡牌出生可拖拽**：1.0s 聚合完成后可拖拽，assembly 代码不在拖拽时渲染
- [x] **SHATTER 阶段文字**：读卡器销毁时逐行文字随扫描线淡出

### PIP 动画
- [x] **动画 1 INSERT+SHATTER**合并为：边滑动边粉碎（0.8s），文本逐行淡出
- [x] **展示碎片聚合**：独立 `pipShowFrags` 状态，完整纹理画在碎片底层消除分割线
- [x] **文字使用擦除进度**：PIP 两处文字改用进度阈值替代纹理映射
- [x] **衰减缓冲区**：`pipDecayFrags` 状态切换后继续淡出

### Bug 修复
- [x] 疫病死亡率：敌方 10%，我方 5%（从统一 20% 拆分）
- [x] 按钮抖动：去掉 1/60s 时钟限制，每帧刷新；振幅 ±1px
- [x] 紫卡传送触发 bttle_02 + 延长危机

## 备注
- `yiDiShiQin` 状态机：Active → Responded/Penalizing → Penalty=0 移除
- 惩罚倒计时在 `processPurpleCurses` 开头统一递减
- AI 回应在 `isAiThinking` 设为 true 之前判断，保证单回合只能择一行动
